export class ProcessText {
    constructor(text) {
        this.processingModules = [];
    }

    addNewProcessingModel(processModule) {
        this.processingModules.push(processModule);
    }

    startProcessing() {
        for (let i = 0; i < this.processingModules; i++) {
            this.processingModules[i].
        }
    }
}